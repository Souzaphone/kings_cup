# Admin — Kings Cup

No admin section currently. The game host has limited privileges (can start/reset the game) but there is no admin panel or elevated access.

When user accounts are added, consider adding a host-verification check so only the actual game creator can start/reset games.
